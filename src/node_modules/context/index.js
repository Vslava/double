require('intel').console();
require('../../config/init-dotenv');

const databaseConfig = require('../../config/database');
const models = require('../../models')(databaseConfig);
const settings = require('../../config/settings');

module.exports = {
  models,
  settings,
};
