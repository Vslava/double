require('intel').console();
require('../../config/init-dotenv');

const databaseConfig = require('../../config/database');

module.exports = () => ({
  settings: require('../../config/settings'),
  models: require('../../models')(databaseConfig),
  services: require('../../services'),
});
